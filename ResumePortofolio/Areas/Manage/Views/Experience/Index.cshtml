@model List<Experience>
@{
    ViewData["Title"] = "Index";
}

<div>
    <a asp-controller="Experience" asp-action="Create" >
        Create
    </a>
</div>
<table class="table">
    <thead>
        <tr>
            <th scope="col">#</th>
            <th scope="col">FirstName</th>
            <th scope="col">LastName</th>
            <th scope="col">Address</th>
            <th scope="col">PhoneNumber</th>
            <th scope="col">Email</th>
            <th scope="col">Title</th>
            <th scope="col">Setting</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <th scope="row">@item.Id</th>
                <td>@item.SubTitle</td>
                <td>@item.Title</td>
                <td>@item.Info</td>
                <td>@item.dateTime</td>
               
                <td>
                    @if (item.Title.Length > 30)
                    {
                        @(item.Title.Substring(0,30)+"...")
                    }
                    else
                    {
                        @item.Title
                    }
                </td>
                <td>
                    <a asp-controller="Experience" asp-action="Update" asp-route-id="@item.Id" class="btn btn-primary">
                     Update
                    </a>
                    <a asp-controller="Experience" asp-action="Delete" asp-route-id="@item.Id" class="btn btn-outline-danger ">
                        Delete
                    </a>
                </td>

            </tr>
        }

    </tbody>
</table>